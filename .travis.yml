language: node_js
node_js:
  - node
sudo: false
cache:
  yarn: true
env:
  CI=true

before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.22.0
  - export PATH="$HOME/.yarn/bin:$PATH"

jobs:
  include:
    - stage: test
      script: yarn test
